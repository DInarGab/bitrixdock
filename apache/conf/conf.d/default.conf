<VirtualHost *:80>
	ServerAdmin webmaster@localhost

	DocumentRoot /var/www/localhost/htdocs
	ProxyPassMatch ^/(.*\.php(/.*)?)$ fcgi://php:9000/var/www/localhost/htdocs/$1
	ProxyTimeout 300
	ProxyPassMatch ^/(fpm-ping|fpm-status) fcgi://php:9000 

	<IfModule dir_module>
	    DirectoryIndex index.html
	</IfModule>

	<Files ".ht*">
	    Require all denied
	</Files>

	<Directory />
		Options FollowSymLinks
		AllowOverride None
	</Directory>

	<Directory "/var/www/localhost/htdocs/">
	    AllowOverride All
	    Options None
	    Require all granted
	</Directory>

	ErrorLog /var/log/apache-error.log

	# Possible values include: debug, info, notice, warn, error, crit,
	# alert, emerg.
	LogLevel warn

	CustomLog /var/log/apache/apache-access.log "%h %l %u %t \"%r\" %>s %b \"%{Referer}i\" \"%{User-agent}i\""

</VirtualHost>